#!/usr/bin/env ruby

require_relative "../lib/play"

flag = true
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

  puts "Welcome to Tic Tac Toe!"
  display_board(board)

  # Make sure to execute #play below
  play(board)
  

  #puts "new game? (Y/N)"
  #inputNewGame = gets.strip
  #if (inputNewGame == "n" || inputNewGame == "N" || inputNewGame == "no" || inputNewGame == "No" || inputNewGame == "NO")
  #  flag = false
  #elsif (inputNewGame == "y" || inputNewGame == "Y" || inputNewGame == "yes" || inputNewGame == "Yes" || inputNewGame == "YES")
  #  flag = true
  #else
  #  puts "Not recognised"
  #  flag = false
  #end
